**What does this change?**

**Why?**

**Tested how?**
(Real hardware only — this can't be meaningfully tested in CI. Note the
laptop/sensor you tested on, and what you checked: image looks right,
CPU/memory stay flat over a few minutes, etc.)

---
- [ ] Target branch is `dev`, not `main` (`main` only gets fast-forwarded from `dev` for releases)
